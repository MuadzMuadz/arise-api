import { Injectable } from '@nestjs/common';

/**
 * AuthService — register, login, verify-email orchestration.
 * Method implementations di-tambahin per Step 5-10 (lihat
 * docs/auth/implementation-plan.md § 9).
 */
@Injectable()
export class AuthService {}
